# 赛道5赛题二：方法优化清单

> 基于实验结果，整理可优化的方法及具体步骤

**更新时间**：2026-05-15

---

## 当前实验结果

| 方法 | 精度 | 状态 | 优化空间 |
|------|------|------|---------|
| 基线 | 81.0% | — | — |
| P2-1 STE baseline | 83.84% | ✅ 完成 | 可增加epochs |
| P2-2 gradient_variance | ❌ 失败 | 设备不匹配 | 修复后可用 |
| P2-3 zero_order_correction | ❌ 失败 | 设备不匹配 | 修复后可用 |
| P2-4 spatiotemporal_noise | 80.25% | ⚠️ 低于基线 | 需调参 |
| P2-5 decoupled_bias | 80.15% | ⚠️ 低于基线 | 需调参 |
| P2-6 regularizer_v2 | 51.09% | ❌ 崩溃 | 大幅调参 |

---

## 优化优先级

### 🥇 第一优先级（修复即有效）

| 方法 | 问题 | 修复难度 | 预估提升 |
|------|------|---------|---------|
| **P2-2 gradient_variance** | 设备不匹配 | 低 | +1~3% |
| **P2-3 zero_order** | 设备不匹配 | 低 | +2~3% |

**原因**：这两个方法与P2-1原理相同，失败原因是代码bug而非方法无效。修复后大概率达到+2%以上的提升。

**修复步骤**：
```python
# ste/core.py 已部分修复（inject_to_weight/inject_to_input）
# 还需修复 NoisyConv2d/NoisyLinear 的 forward 中的设备处理
```

---

### 🥈 第二优先级（调参即可）

| 方法 | 问题 | 修复难度 | 预估提升 |
|------|------|---------|---------|
| **P2-4 spatiotemporal** | 噪声强度不当 | 中 | +1~2% |
| **P2-5 decoupled_bias** | 偏置校正权重不当 | 中 | +1~2% |

**优化步骤**：
- P2-4：降低 `noise_strength` 从 1.0 到 0.1-0.3
- P2-5：调整 `bias_correction_factor` 和 `learning_rate`

---

### 🥉 第三优先级（需要大改）

| 方法 | 问题 | 修复难度 | 预估提升 |
|------|------|---------|---------|
| **P2-6 regularizer_v2** | 正则化权重过大 | 高 | +0~2% |

**问题**：`lambda_reg=0.1` 导致 loss=127，准确率仅51%
**优化步骤**：
1. 降低 `lambda_reg` 到 0.001-0.01
2. 使用梯度裁剪防止loss爆炸
3. 增加 warmup 阶段

---

## 推荐执行顺序

1. **修复 P2-2/P2-3**（1-2小时）→ 预期 +2~3%
2. **优化 P2-4/P2-5**（2-3小时）→ 预期 +1~2%
3. **调整 P2-6**（3-4小时）→ 预期 +0~2%

**如果时间有限**：只做第一步（修复P2-2/P2-3）即可获得完整的有效实验结果。
