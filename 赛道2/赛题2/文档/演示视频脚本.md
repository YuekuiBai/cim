# 赛道二：编译工具链 - 赛题二 演示视频脚本

## 视频要求
- **格式**：MP4
- **时长**：5 分钟以内
- **内容**：实现全流程、优化排布思路及优化前后效果对比

---

## 视频脚本（建议时长：4 分 45 秒）

### 第一部分：项目介绍（0:00 - 0:30）

**画面**：项目封面 + 赛题背景

**旁白**：
> 各位评委好，我们团队完成了赛道二赛题二的权重排布优化。本项目面向 3D 存算一体硬件，设计了一套智能编译器/调度器框架，能够将深度学习模型特别是 MoE 大模型高效映射到分层 3D 异构资源空间。

---

### 第二部分：硬件模型与挑战（0:30 - 1:00）

**画面**：展示图5.png（3D 资源空间）+ 核心约束说明

**旁白**：
> 硬件模型采用三级层级结构：全局空间包含 N×N 个 Sub-Cubes，每个 Sub-Cube 内含多个 Weight-Cubes。核心约束包括：权重驻留不可迁移、同一时刻仅激活一个 Weight-Cube、Sub-Cube 内切换产生 D 周期延迟。

---

### 第三部分：编译映射框架（1:00 - 1:45）

**画面**：展示框架流程图 + 代码目录结构

**旁白**：
> 我们的框架包含六个核心模块：模型解析、权重切分、MoE 激活轨迹分析、3D 映射算法、流水线调度和硬件模拟。数据流从模型文件开始，经过解析和切分，通过智能映射算法生成 3D 排布方案，最后进行调度优化和约束验证。

**操作演示**：
- 展示代码目录结构
- 展示 main.py 主入口

---

### 第四部分：MoE 激活轨迹分析（1:45 - 2:30）

**画面**：展示 trace_analysis.json + 共现矩阵可视化

**旁白**：
> 针对 MoE 模型，我们设计了激活轨迹分析模块。通过统计 Expert 的共现频率，构建共现矩阵，识别互斥 Expert 对和频繁协同工作的专家组。基于分析结果，我们采用频率感知放置策略，将高频 Expert 放置在低切换开销位置。

**操作演示**：
- 展示 activation_trace.json 文件格式
- 展示 trace_analysis.json 分析结果

---

### 第五部分：3D 映射算法（2:30 - 3:15）

**画面**：展示图4.png（权重排布）+ solution.json 映射结果

**旁白**：
> 核心算法采用 Greedy First-Fit 策略，结合优先级排序。大权重优先放置以减少空间碎片，MoE 高频 Expert 优先以优化切换开销，关键路径算子优先以降低端到端延迟。算法自动适配深度需求，避免深度溢出。

**操作演示**：
- 展示 solution.json 中的 3D 坐标分配
- 解释 Sub-Cube 选择和 z 位置计算

---

### 第六部分：流水线调度（3:15 - 3:45）

**画面**：展示调度甘特图 + 依赖图

**旁白**：
> 调度模块基于依赖图进行拓扑排序，采用 Barrier 同步策略确保下游算子等待上游所有 Section 就绪。利用 Sub-Cube 间并行性，实现多层流水线并行执行，最大化硬件利用率。

---

### 第七部分：实验结果（3:45 - 4:20）

**画面**：展示测试结果表格 + 对比数据

**旁白**：
> 实验结果方面，我们成功处理了简单模型和 MoE 模型。简单模型实现 100% 空间利用率，MoE 模型通过激活轨迹分析优化后，端到端延迟显著降低。所有约束验证均通过。

**操作演示**：
- 运行 run_all_tests.py
- 展示两个模型的编译输出
- 展示 solution.json 验证结果

---

### 第八部分：优化对比与总结（4:20 - 4:45）

**画面**：优化前后对比表格 + 总结页面

**旁白**：
> 优化对比显示，我们的策略有效减少了空间碎片，实现了负载均衡，并通过频率感知放置降低了切换开销。总结来说，我们实现了一套完整的面向 3D 存算一体硬件的智能编译映射与调度框架。感谢各位评委的聆听！

---

## 录制建议

1. **屏幕录制**：使用 OBS 或系统自带录屏工具
2. **代码展示**：使用 VS Code 或 IDE，字体放大至 16pt 以上
3. **终端演示**：使用清晰的终端配色，命令执行过程完整展示
4. **旁白录制**：使用专业麦克风，语速适中，发音清晰
5. **后期剪辑**：使用剪映或 Premiere，添加转场和字幕

## 关键演示点

| 时间点 | 演示内容 | 预期效果 |
|--------|---------|---------|
| 1:15 | 代码目录结构 | 展示模块化设计 |
| 1:50 | trace_analysis.json | 展示 MoE 分析能力 |
| 2:40 | solution.json 映射 | 展示 3D 排布结果 |
| 3:30 | 调度甘特图 | 展示流水线并行 |
| 4:00 | run_all_tests.py | 展示全部模型映射成功 |
