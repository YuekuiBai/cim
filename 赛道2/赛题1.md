# 赛道二：编译工具链 - 赛题一

## 赛题背景及意义

随着大模型技术的快速演进与边缘计算的普及，传统冯·诺依曼架构正面临"**内存墙**"与"**能耗墙**"的双重挑战。在这一背景下，存算一体架构以其高能效比与高计算密度的优势，成为突破计算瓶颈的关键技术，尤其适用于对能效和实时性要求极高的边缘 AI 部署场景。

当前，以**NAND FLASH**为代表的新型非易失性存储介质因其高密度、低功耗、低成本的特性，正被广泛应用于存算一体芯片的设计中。然而，如何将复杂的神经网络高效部署到基于 NAND FLASH 的存算一体硬件上，仍是一个涉及编译器、架构与算法协同优化的系统工程难题。其中，**编译器的设计与优化**是实现高性能、高利用率、可扩展部署的核心环节。

本赛题旨在引导参赛队伍深入理解存算一体硬件特性与 NAND FLASH 计算机制，设计并实现一套面向存算阵列的编译器中间表示与编译框架，完成从神经网络模型到存算指令的高效映射与调度，推动存算一体技术在大模型边缘部署中的落地应用。

---

## 赛题任务

针对存算一体部署的关键指标，搭建神经网络算法到存算一体硬件部署的简化编译框架，包括但不限于以下三个环节：

### 1. IR 设计

设计适合存算阵列特性的中间表示，支持 linear，elementwise 算子，并能表达数据依赖、存储位置等信息。

### 2. 模型解析

完成 onnx 模型导入，转换为等价的 IR 表达。

### 3. 指令生成

将抽象的 IR 语义，降级为存算一体架构的硬件 ISA。在降级过程中，需要同时完成以下硬件资源管理：

- **a. 权重映射：** linear 算子的权重数据需要映射到存算单元中。权重可复制，可拆分，以达到优化加速的目的。
- **b. SRAM 资源管理：** linear，elementwise 算子的输入数据、输出数据均位于 SRAM。Linear 算子的 bias 常量数据需要在 SRAM 规划空间。

---

## 赛题要求

参赛队伍需实现神经网络模型到存算阵列的部署框架。具体要求如下：

| 序号 | 要求 | 说明 |
|:---:|------|------|
| 1 | 不限编程语言和算法 | 可自建工程也可基于开源项目进行二次开发，推荐使用 **MLIR 编译框架** |
| 2 | 保留中间结果 | 需要保留部署过程中各环节的中间结果，中间结果文件格式可自定义 |
| 3 | ONNX 模型编译通过 | 赛题包中的 ONNX 模型均需要编译通过<br>赛题包链接：`https://resources.modelscope.cn/third-part/files/存算一体编译架构设计赛题包.zip` |

### ONNX 模型约束

| 约束项 | 详细说明 |
|-------|---------|
| 算子类型 | 模型中仅存在 **linear** 和 **elementwise**（add/sub/mul/div）两种算子 |
| 数据类型 | 模型中所有算子的数据类型均为**整形** |
| ISA 数据类型 | 指令集 ISA 操作的所有数据类型均为**整形** |
| 数据 Layout | 所有算子输入输出数据 layout 固定，且与硬件计算要求一致 |
| 计算范围 | 默认所有计算结果均在合法范围内，不需要考虑 overflow 及 truncate 产生的误差 |

---

## 目标存算一体硬件信息

### 1. 指令集

目标硬件支持以下 3 条基础指令，参赛者可根据架构设计需求，自定义扩展指令（自定义指令需由参赛者准确且清晰地描述语义，参考基础指令）。

#### 基础指令 1：cim.bit.type

```
cim.bit.type dst, src, index, weight_pos
```

| 参数 | 说明 |
|------|------|
| 指令描述 | 输入数据位于地址 src，输入和权重的数据类型均为 type，可选 i8，i16，i32。对输入数据的第 index 个 bit 进行存内计算，输出到地址 dst，输出数据的数据类型为 i32 |
| out | 输出数据在 SRAM 中的起始地址 |
| in | 输入数据在 SRAM 中的起始地址 |
| index | 计算输入数据中的第几个 bit |
| weight_pos | 权重数据在存算单元中的坐标位置，表示为 [row0, col0, row1, col1] |

#### 基础指令 2：elt.op.type.mode

```
elt.op.type.mode dst, src1, src2, len
```

| 参数 | 说明 |
|------|------|
| 指令描述 | Elementwise 计算：<br>① op 表示计算类型，可选 add/sub/mul/div<br>② type 表示参与计算的输入数据和输出数据类型，可选 i8/i16/i32<br>③ mode 表示 src1 和 src2 数据的维度，可选 vv（src1 和 src2 均为一维向量），vi（src1 为一维向量，src2 为立即数） |
| dst | 输出数据在 SRAM 中的起始地址 |
| src1 | 输入数据 1 在 SRAM 中的起始地址 |
| src2 | 输入数据 2 在 SRAM 中的起始地址 |
| len | 参与计算的元素个数 |

#### 基础指令 3：mem.copy.dstType.srcType

```
mem.copy.dstType.srcType dst, src, len
```

| 参数 | 说明 |
|------|------|
| 指令描述 | 将 src 地址的数据拷贝至 dst 地址，拷贝元素数量为 len，支持随路类型转换：<br>① dstType 表示目的数据的类型，可选 i8/i16/i32<br>② srcType 表示源数据的类型，可选 i8/i16/i32 |
| dst | 目的数据在 SRAM 中的起始地址 |
| src | 源数据在 SRAM 中的起始地址 |
| len | 参与计算的元素个数 |

### 2. 2D 存算单元规格

> **规格：** `[row: 1024bit, col: 4096bit]`

知存科技的模拟存算计算方案（如图 1），首先将被乘矩阵的参数提前存入存算单元，然后将乘数向量输入，在水平方向上，乘数在存算单元中完成与被乘数的乘法操作，在垂直方向上，每个存算单元的乘法结果累加，最后得到输出向量。

<div align="center">
  <img src="图1.png" width="500" alt="知存模拟存算计算方案原理图"/>
  <p>图 1 知存模拟存算计算方案原理图</p>
</div>

### 3. SRAM 规格

> **容量：** 512KB

---

## 预期编译结果示例

### 示例模型

若输入为图 2 所示的 linear 算子（无 bias），数据类型及 layout 如下：

| 张量 | 数据类型 | Layout |
|------|:-------:|--------|
| Input | int8 | [batch, 1, Cin] |
| B | int8 | [Cin, Cout] |
| output | int32 | [batch, 1, Cout] |

<div align="center">
  <img src="图2.png" width="500" alt="No-bias linear ONNX"/>
  <p>图 2 No-bias linear ONNX</p>
</div>

### 资源分配示例

一种可行的资源分配结果如图 3 所示：

**SRAM 布局：**

| 地址范围 | 用途 | 大小 |
|---------|------|------|
| 0 ~ 127 | input | 128B |
| 128 ~ 639 | output | 512B |
| 640 ~ 1151 | acc | 512B |
| 1152 ~ 1663 | tmp | 512B |

**存算单元映射：**

| 映射项 | 值 |
|-------|---|
| B 权重 | 128 × 1024 |
| [Cin] 方向 | row 方向 |
| [Cout × 权重元素位宽] 方向 | col 方向 |

<div align="center">
  <img src="图3.png" width="500" alt="资源分配示意图"/>
  <p>图 3 资源分配示意图</p>
</div>

### 预期生成的指令序列

```assembly
// 取 input 所有元素的第 0 个 bit，做存内计算
cim.bit.i8 /*acc*/640, /*input*/0, /*index*/0, /*weightPos*/[0, 0, 128, 1024]

// 取 input 中所有元素的第 1 个 bit，做存内计算，结果左移 1 位，与前几个 bit 的结果相加
cim.bit.i8 /*output*/128, /*input*/0, /*index*/1, /*weightPos*/[0, 0, 128, 1024]
elt.mul.i32.vi /*tmp*/1152, /*output*/128, /*src2*/2, /*len*/128
elt.add.i32.vv /*acc*/640, /*acc*/640, /*tmp*/1152, /*len*/128

// 取 input 中所有元素的第 2 个 bit，做存内计算，结果左移 2 位，与前几个 bit 的结果相加
cim.bit.i8 /*output*/128, /*input*/0, /*index*/2, /*weightPos*/[0, 0, 128, 1024]
elt.mul.i32.vi /*tmp*/1152, /*output*/128, /*src2*/4, /*len*/128
elt.add.i32.vv /*acc*/640, /*acc*/640, /*tmp*/1152, /*len*/128

// 取 input 中所有元素的第 3 个 bit，做存内计算，结果左移 3 位，与前几个 bit 的结果相加
cim.bit.i8 /*output*/128, /*input*/0, /*index*/3, /*weightPos*/[0, 0, 128, 1024]
elt.mul.i32.vi /*tmp*/1152, /*output*/128, /*src2*/8, /*len*/128
elt.add.i32.vv /*acc*/640, /*acc*/640, /*tmp*/1152, /*len*/128

// 取 input 中所有元素的第 4 个 bit，做存内计算，结果左移 4 位，与前几个 bit 的结果相加
cim.bit.i8 /*output*/128, /*input*/0, /*index*/4, /*weightPos*/[0, 0, 128, 1024]
elt.mul.i32.vi /*tmp*/1152, /*output*/128, /*src2*/16, /*len*/128
elt.add.i32.vv /*acc*/640, /*acc*/640, /*tmp*/1152, /*len*/128

// 取 input 中所有元素的第 5 个 bit，做存内计算，结果左移 5 位，与前几个 bit 的结果相加
cim.bit.i8 /*output*/128, /*input*/0, /*index*/5, /*weightPos*/[0, 0, 128, 1024]
elt.mul.i32.vi /*tmp*/1152, /*output*/128, /*src2*/32, /*len*/128
elt.add.i32.vv /*acc*/640, /*acc*/640, /*tmp*/1152, /*len*/128

// 取 input 中所有元素的第 6 个 bit，做存内计算，结果左移 6 位，与前几个 bit 的结果相加
cim.bit.i8 /*output*/128, /*input*/0, /*index*/6, /*weightPos*/[0, 0, 128, 1024]
elt.mul.i32.vi /*tmp*/1152, /*output*/128, /*src2*/64, /*len*/128
elt.add.i32.vv /*acc*/640, /*acc*/640, /*tmp*/1152, /*len*/128

// 取 input 中所有元素的第 7 个 bit，做存内计算，结果左移 7 位，与前几个 bit 的结果相减
cim.bit.i8 /*output*/128, /*input*/0, /*index*/7, /*weightPos*/[0, 0, 128, 1024]
elt.mul.i32.vi /*tmp*/1152, /*output*/128, /*src2*/128, /*len*/128
elt.sub.i32.vv /*acc*/640, /*acc*/640, /*tmp*/1152, /*len*/128

// 将累加的结果拷贝至 output
mem.copy.i32.i32 /*output*/128, /*acc*/640, /*len*/128
```

---

## 赛题评分

### 1. 提交内容要求

#### 1) 设计报告

参赛队伍需提交设计报告（doc/docx 及 pdf 文件），内容需包含：

| 章节 | 内容要求 |
|------|---------|
| a) 引言 | 研究背景与意义 |
| b) 工作原理与关键技术原理分析 | 包括基本概念、处理流程以及数学建模等；结构设计：包括结构选择、模块划分、技术选型、接口描述等 |
| c) 详细设计与实现 | 包括软件流程图、关键代码分析等、优化排布论证与分析 |
| d) 程序运行效果与实验结果 | 实验数据与结果分析 |
| e) 总结 | 工作总结与展望 |
| f) 参考文献和团队介绍 | 学术规范与团队信息 |

#### 2) 介绍 PPT

主要工作、创新点、结果、结论。

#### 3) 演示视频

提交演示视频一份（MP4 格式）：

- 时长控制在 **5 分钟以内**
- 视频主要展示实现的全流程、优化排布思路及优化前后效果对比
- 视频讲解清晰完整，演示过程流畅，数据展示有说服力

#### 4) 设计代码

工程代码应该与设计报告中的详细设计相匹配。

---

### 2. 评判标准

| 评分模块 | 评分项 | 分值 |
|---------|-------|:----:|
| 部署框架 | IR设计（表达能力、简洁性、扩展性） | 20 |
| | 模型解析（完成 onnx 模型到 IR 转换） | 10 |
| 指令生成 | 生成正确的指令序列 | 15 |
| | 资源管理策略 | 15 |
| 整体实施方案 | Transform/Lowering | 20 |
| 技术路线 | 在实现过程中，如果在技术路线上有成功的改进或突破，可以根据技术路线的先进性判别得分 | 10 |
| 设计报告 | 应详细阐述技术方案，突出关键技术要点，报告应结构清晰、逻辑流畅、详略得当 | 5 |
| 现场答辩 | 讲解思路清晰，语言流畅 | 5 |
| **总分** | | **100** |

---

*文档更新时间：2026-05-08*  
*信息来源：官方赛题说明*
